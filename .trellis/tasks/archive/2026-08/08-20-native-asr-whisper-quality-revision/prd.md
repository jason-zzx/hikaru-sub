# Revise native Faster-Whisper quality

## Goal

建立新的 ordinary Faster-Whisper native candidate，恢复首个 native release 的 mandatory route，并让 `large-v3` 与 `large-v2` 两个独立 anchor 都通过同模型 `python-legacy-cuda-v1` 字幕质量非回退矩阵和既有绝对硬门禁。

## Final Disposition

`stop-revise / non-qualified`

本任务未建立正式 candidate。三个有效 model-backed diagnostic scope 均发布 `no-candidate-selected`；后续 Phase 3D 未产生 feature/runtime/parser attribution，因为冻结的 `80 × 3000` Mel authority 与 exact large-v3 模型实际 `128` Mel contract 不兼容。Production/default 保持 Python legacy，ordinary native Faster-Whisper 保持 disabled；无正式双 anchor 六行矩阵、无 accepted algorithm handoff、无 CPU `inherited-from-gpu` metadata。Truthful handoff 位于 `research/whisper-quality-non-qualified-handoff.md`，后续工作移交独立 planning task `08-25-native-asr-whisper-execution-parity-discovery`。

## Confirmed Evidence

- 当前前瞻性质量 authority 已将历史 `selected-cpu-beam1-no-history` large-v3 Candidate A 判为 `stop-revise`，但其结构、timeline、semantic-gap、CPU RTF、RSS、路径和协议门禁仍通过；本任务解决的是逐项字幕文本质量回退，不重做 worker/host 架构。
- Candidate A 相对 large-v3 Python baseline 的已观察回退为：short-v1 deletions `18 > 16`、insertions `3 > 2`；medium-v1 CER `0.1134 > 0.1002`、substitutions `78 > 72`、deletions `149 > 92`；long-v2 deletions `1906 > 1686`。其余相对质量字段通过。
- 历史 beam-5 short-v1 row 的 S/D/I 为 `25/16/2`，与 large-v3 Python baseline 完全相同；历史 full-history beam-5 medium-v1 row 的 S/D/I 为 `89/88/49`，仅 substitutions 劣于 baseline `72/92/64`，但 CPU inference RTF `1.100` 失败。
- 历史同二进制 120 秒诊断表明 timestamp-driven/no-history/beam-5 CPU RTF 为 `0.916`，低于 `1.0` 门槛，但该 cell 不是 authoritative quality row。它是当前最小单变量新候选的主要证据，不构成资格结论。
- T07 已证明相同 worker/backend 可在 RTX 3070 CUDA FP16 上运行；本任务的诊断和正式模型矩阵都使用该 GPU 通道。按用户批准的 `native-gpu-authoritative-v1` 规则，匹配同模型、同算法 identity 的 CPU 路线继承 GPU 资格，不采集或推断 CPU CER/S/D/I/RTF/RSS；报告必须标记 `inherited-from-gpu`，不得复制 GPU 数值冒充 CPU 测量。T07 artifact 仍不构成最终 pack identity，正式 pack/device attestation 归 T14/T15。
- large-v2 尚无可复用的完整 native quality row；其 short-v1、medium-v1、long-v2 必须与 large-v3 使用同一冻结候选策略独立获取。Python large-v2 long-v2 的 V4/seed/session 路径只是强制回归对象，不是 native 实现模板。
- 首轮 GPU Gate A 已按冻结六格完成并发布 `no-candidate-selected`（tracked JSON SHA-256 `49e6add03a2630298df3b673a0ddfb3c0b0ad1ee65a822df62ef482b2fb5177f`）。所有格均通过 GPU RTF/RSS；失败分布为：`short-b1-off` 仅 deletions `27 > 16`，`short-b5-off` 仅 insertions `6 > 2`；`medium-b1-off` 的 CER/S/D 为 `0.103736/83/119 > 0.100220/72/92`，`medium-b5-off` 为 `0.103297/89/117`；`medium-b1-on` 以完整 trace 失败 `timestamp_after_audio`；`medium-b5-on` 的 CER/S/D/I 均通过，但出现 1 个 `2350ms` semantic confirmed-speech gap。诊断 row 仅用于选择且不得晋升。
- `medium-b5-on` 的 gap 位于正常 30 秒生成窗口内部；该窗未 no-speech、未异常 seek，说明它是 history-conditioned generation coverage 缺口而非 publisher、timeline 或外部终止问题。`medium-b1-on` 的失败发生在最后 `1512ms` source window，模型生成了 WAV 末尾之后的时间戳。
- 归档 Candidate B 只测过 `beam1/history-off + exact faster-whisper 1.2.1 Silero V6 VAD`。以当前 corpus/reference/comparator 重算，其 short/medium semantic gap 均为 0，但 short deletions/insertions `18/3 > 16/2`，medium CER/deletions `0.105495/119 > 0.100220/92`，因此不能作为当前候选；它只证明 exact Silero V6 seam 可复用并可能改变 coverage。
- 用户批准的第二轮 exact-VAD GPU 诊断已在独立 lock `ee8a7e6bfd062f84359eaa3d86eb54ed843bb4d036613043ed2bc2f8f282b8c3` 下完成并再次发布 `no-candidate-selected`（tracked JSON SHA-256 `890453f440418d4c418e7916fcbe52b8ea9db2c85f48ef79b7d1b30bc372ecbc`）。`short-b5-vad` 与无 VAD beam5 short 完全相同，为 CER `0.333333`、S/D/I `19/15/6`、gap 0，仍因 insertions `6 > 2` 失败；其 VAD 保留全部 `385637/385637` samples。
- `medium-b5-on-vad` 消除了无 VAD row 的 `2350ms` semantic gap，但文本质量从 CER/S/D/I `0.092747/69/85/57` 退化到 `0.126593/84/112/92`，相对 Python baseline `0.100220/72/92/64` 的 CER、S、D、I 全部失败。VAD 保留 `7593600/7981952` samples、10 个 intervals；GPU RTF `0.118600`、RSS 约 `3.01GiB`、timeline 和恢复 provenance 均通过。因此 exact VAD 解决 coverage 缺口但引入更大的文本回退，不能作为正式候选。
- 用户批准的非资格 upstream parity 研究已完成，产物为 `research/upstream-parity-audit.md`（SHA-256 `353d93e2a2951a1168b42aa55e178ef13be9a4d875f6fc7da623890e3de63abd`），未运行模型推理。研究确认 large-v3 三个 Python baseline case 都走 upstream `WhisperModel` direct path：beam 5、exact V6 VAD，并继承 faster-whisper 1.2.1 的 generation fallback；native 每窗只做一次 temperature 0 generation，缺少 compression-ratio/log-probability retry ladder 和 selected-temperature prompt reset。这是唯一同时存在于 short 单窗口 blocker 与 medium 多窗口失败中的已证实 decode-policy mismatch。
- 研究还确认 native 会把未输出的 timestamp slice 加入 previous-text history，而 upstream 只累计已 yield 的非空正时长 segment。该差异可能影响 medium history 行，却无法解释没有 history 的 short blocker，故不能单独作为下一 candidate。现有 Python baseline 无 per-window fallback telemetry，因此 fallback 是否实际触发、是否改善输出仍只是待验证假设，不构成资格或 acquisition 授权。
- 用户随后明确批准在冻结 lock SHA-256 `5fcfe845451f418a1e21fe8711ba9f367fdc2e13f8c2e02dd598683dde4e073a` 下运行两行 fallback diagnostic。模型加载前的重新 attestation 证明 source/runtime/model/corpus/VAD/GPU/PATH 当前值均匹配，但该 lock 未直接冻结 `vcomp140.dll`、`nvcuda.dll`、`cublas64_12.dll`、`cublasLt64_12.dll` 的 SHA-256；冻结 publisher 又要求每个 loaded-module hash 必须直接出现于当前 input lock。因此 evidence 必然以 `loaded module is not frozen` 失败，按 invalid-evidence 规则在模型加载前停止。两行均未运行、无质量/性能/fallback metrics、无诊断 publication，lock 未修改。
- corrected Phase 3C v2 identity 冻结为 `research/gpu-fallback-diagnostic-lock-v2.md`，SHA-256 `823630f08ab15ed7dadf4f80f0e5a320c89e3e2171cd55b8ac86cc6463235b82`。它直接列出完整 expected loaded-module inventory，且 51 个 source/tool/runtime/model/corpus/VAD/zlib/authority 文件无 drift；但独立 Gate C3 review 发现 publisher 仍只要求 module 子集，可能接受缺少 `vcomp140.dll` 或额外 frozen module 的 row，因此 v2 被阻断且未授权 acquisition。
- Gate C3 已修复 fallback-only exact module-set validator，并冻结 `research/gpu-fallback-diagnostic-lock-v3.md`，SHA-256 `b95bdcc1e2a33ab30c2064fef70d466cd92842fedaaaed07350a1c111bcdcf29`。fallback row 现在必须精确匹配八个 module 的 name/size/SHA-256/root-role/raw-version；missing/additional/identity/version/root drift 均拒绝。Publisher tests `15/15` 通过，旧 lock 与 invalid/v2 preflight artifacts 保持 byte-identical。Gate C3 结论为 `clean-to-request-renewed-acquisition`。
- 用户随后明确批准 v3 两行 acquisition。模型加载前重新 attestation 通过 55 个冻结文件、八个 exact modules、RTX 3070/driver/CUDA/MSVC/CMake identity。`short-b5-on-vad-fallback` 未触发 fallback（1 generation、0 fallback），质量与第二轮 short row 相同：CER `0.333333`、S/D/I `19/15/6`、gap 0，仍以 insertions `6 > 2` 失败；GPU RTF `0.116202`、RSS `3232940032` bytes。`medium-b5-on-vad-fallback` 在 17 个 windows 中执行 29 generation calls / 12 fallback calls，selected temperatures 为 `0.0×13, 0.2×1, 0.4×1, 0.8×1, 1.0×1`；但 CER/S/D/I `0.138901/84/167/65` 与 2 个 semantic gaps / `3930ms` 均不劣规则失败，GPU RTF `0.195917`、RSS `3232133120` bytes。确定性 publication 为 `whisper-gpu-fallback-diagnostic.json` SHA-256 `66dfb00ee41599f78dc3cdf2c757d0946a922988e62bcc016b5d7c674c345f7c` 与 Markdown `8766aeedd545ec5b4cb2f79144320f730c6d0692ba1d51429ba78740883a443c`，结论 `no-candidate-selected`。首次 medium 进程受外部 harness timeout 终止且无 atomic output，按 invalid-evidence 规则未使用；随后仅重跑该 row 并成功发布。
- 后续 source/evidence-only audit 记录于 `research/next-candidate-causal-audit.md`，SHA-256 `fcebb8c3c5665a9cd1f5adfbcf18431e235cfdfbab9bbc0f55a56893a59b91b0`，未运行 ASR 推理。short 解码 float32 waveform 已证明 Python/native byte-identical，prompt/suppression/options 也无已证实差异；但完整 `80×3000` Mel tensor 并非 byte-identical（max abs `7.51e-6`），且 Python wheel CT2 `4.8.0` 与 native no-cuDNN CT2 `4.8.0` 的 DLL/module identity 明显不同。归档 Python baseline 缺少 Mel、first-attempt tokens、fallback、loaded-module 与 cross-parser telemetry，因此当前 evidence 不能在 feature/runtime/generation/parser 之间归因，也不能直接冻结下一 candidate。
- 用户已批准 `short-input-runtime-parity-bisect-v1` 的 pre-model 规划与实现：构建 promotion-ineligible 的 `Python/native Mel × Python-wheel/native CT2 runtime` 四格 short-only probe，完成 fake/mutation/privacy tests 与独立 review 后必须再次停止。该批准不包含 ASR model load、四格 acquisition、baseline 重写、quality candidate freeze、production default 修改或正式六行矩阵。
- 同 runner 可行性已通过 no-model planning smoke，记录于 `research/phase3d-planning-feasibility.md`，SHA-256 `15d5398bcdbcd0bda17bd6bc294504f409223acbcf2ad28b7e8e5c439723e7b3`：未改动的 C++ tests executable 在隔离 Python-wheel CT2/cuDNN/OpenMP root 下通过 `--fallback-self-check` 与 `--self-check`。这只证明 ABI/load 与 no-model harness 可行；最终 parity runner 仍须重跑 smoke，未来 model construction/generation/module discovery 仍需单独授权与 fail-closed validation。
- Phase 3D pre-model implementation 已在最终 lock `research/gpu-short-parity-bisect-lock.md` / SHA-256 `470f9e0fba8cb0bc837660eae4173635bcc86212bea83324a02c0953d9c36793` 下完成。相同 runner 在 Python-wheel/native 两个隔离 root 的 no-model smoke 均通过；四个闭集 preflight 均返回 `modelLoaded=false`；parity/protocol/CPU/隔离 CUDA CTest、publisher/oracle mutation/privacy tests、tool/tokenizers/zlib/model/Mel disk identity、task/diff/ignore/no-staged checks均通过。独立 Gate D 复核结论为 `clean-to-request-acquisition`。这只允许请求四格 model-backed acquisition 的新明确授权；当前仍未运行任一格、未生成 parity disposition、未冻结 quality candidate 或修改 production/default。
- 用户明确批准了 lock `470f9e0f...` 下四格 acquisition；但最终模型前检查发现该 lock 只冻结 `vadExpectedInterval`，未直接冻结 Silero V6 asset、`faster_whisper/vad.py`、Python ONNX Runtime identity，也未要求 parity publisher 消费实际 VAD preflight artifact。该授权因此在 VAD/ASR 模型加载前按 invalid-evidence 停止：四格均未运行、无 row/metrics/publication，旧 lock 不修改。
- corrected Phase 3D v2 identity 冻结为 `research/gpu-short-parity-bisect-lock-v2.md`，SHA-256 `481c17cc99d84071bfaf6aaad10222d2848e09416f0b29552e97fa7d42819543`。它直接冻结 CPython、`audio.py`、`vad.py`、Silero V6、Python ONNX Runtime init/PYD/DLL/providers-shared、CPU provider 与 loaded sibling DLL root-role；新增工具先于 import 校验磁盘身份，运行后原子写入 sanitized VAD artifact，publisher 必须重新哈希磁盘/tool 并消费该 artifact 才能读取 parity rows。旧 lock 保持 `470f9e0f...` byte-identical。四个 v2 no-model preflight、publisher `13/13`、oracle/tool `7/7`、既有 GPU publisher `15/15`、task/diff/no-staged 检查通过；独立 Gate D2 结论为 `clean-to-request-renewed-acquisition`。
- 用户随后明确批准 exact v2 VAD preflight 与四格 acquisition。模型/工具/Mel/VAD 磁盘身份在运行前均匹配；Silero V6 实际执行后，冻结 preflight 在 loaded-module attestation 处以 `required loaded module is missing: onnxruntime.dll` fail-closed。冻结 `onnxruntime_pybind11_state.pyd` 的 PE dependency audit 也证明其不直接 import sibling `onnxruntime.dll`，因此 v2 的 expected loaded-module contract 与实际 Python ORT 执行身份不一致。无原子 VAD artifact、无 ASR model load、四格均未运行、无 parser/publication。Ignored sanitized invalid-preflight JSON SHA-256 `5c287a6b7b252284466379873bb4e2a237fda1dc0a1babc551583757ff3fc118`，tracked report SHA-256 `86b7cfcde55d46848ec4003feadfff9b61c7e72e5cbca8eb0bd6658fe137b78d`；v2 lock 仍保持 byte-identical。
- Phase 3D v3 pre-model identity 已冻结为 `research/gpu-short-parity-bisect-lock-v3.md`，SHA-256 `62ef486a9f54d62d3f78504fc293a1917e59746d85cf09a73422edc2c384e42f`。它不改变候选、四格、runner/model/Mel/CT2 runtime 或决策规则，只修正 Python ORT 闭集：import-only smoke 下 exact loaded modules 为 `onnxruntime_pybind11_state.pyd` 与 `onnxruntime_providers_shared.dll`，frozen sibling `onnxruntime.dll` 必须保持未加载；smoke 未构造 `InferenceSession`、未加载 VAD/ASR，ignored JSON SHA-256 `3e3bbbff2aa438a662fb2a2380d6efe688072aebf7dc43a4f07a7d8ca25623e4`。producer 先枚举全部进程模块，任何路径解析失败或 relevant cross-root module 均 fail-closed。独立 Gate D3 最终结论为 `clean-to-request-fresh-v3-acquisition`。
- 用户随后明确批准 exact v3 acquisition。模型前完整 re-attestation 通过，ignored JSON SHA-256 `ad48d75a8a85d4cdf6090cde10b4280653362d91b9876d4b754fb6e9478b38e8`；实际 Silero V6 preflight 也通过并原子发布 SHA-256 `f7201eef4c0e912eb181291038f66942d218ef7fb5e14ff86a0783f62a447246`，重现完整 `[0,385637)`、冻结 waveform、exact loaded `{onnxruntime_providers_shared.dll, onnxruntime_pybind11_state.pyd}` 和 not-loaded `onnxruntime.dll`。但首格 `python-runtime-python-mel` 在 CTranslate2 Whisper model construction 前以冻结错误 `Fallback parity requires beam5/history-on/exact-VAD identity` 退出：reviewed runner 为 fallback-enabled backend 传入 `std::nullopt` VAD path，而 backend 构造器要求 exact-VAD path。无 ASR model load、encode/generate、atomic row、parser 或 publication；其余三格未运行。Ignored invalid record SHA-256 `808dd23c1f69e395bd759aad183e04e6b32e8c354a54f151b64c001e22882132`。V3 授权已消耗且 identity 不得原地修复。
- Phase 3D v4 已完成最小无模型修复并通过独立 Gate D4。新 lock 为 `research/gpu-short-parity-bisect-lock-v4.md`，SHA-256 `e38e23f5fc28405ffb2554324b2967c077d6eeda46b010ec51aac5bece378ff8`：task-local parity runner 的 preflight/acquisition mode 现在强制接收 active isolated runtime bin 内的 exact `silero_vad_v6.onnx`，并在 backend construction 前验证 canonical containment、文件名、`1245151` bytes 和 SHA-256 `4cbf549b8326f60f80f2536d9eefeb450a9abe83365a098031c89719f1be17d2`；production fallback invariant 未放宽。两个 runtime smoke、四个 closed preflight、protocol/CPU/CUDA CTest、publisher/oracle mutation/privacy checks 均通过，四格均明确 `vadModelLoaded=false` / `asrModelLoaded=false` / `modelLoaded=false`。独立 review 结论为 `clean-to-request-fresh-v4-acquisition`；它不授权 VAD 或 ASR inference。
- 用户随后明确批准 exact v4 VAD preflight 与四格 acquisition。模型前 re-attestation 通过 exact v4 lock/source/tool、两个 runtime 闭集、Mel/model/corpus/VAD、restricted PATH、RTX 3070/driver/CUDA 和 predecessor immutability；ignored JSON SHA-256 `385353870ebba5d20c894daf595e80f735ff95d0e629992a7dc52c2fde46b5ef`。四个 no-model preflight 也重新产生与 Gate D4 byte-identical 的 envelope。但新 exact-v4 VAD 命令在任何 faster-whisper/ORT import 或 VAD inference 前以冻结错误 `VAD preflight input path identity drifted` 退出：冻结 tool SHA-256 `21a1e0a2...` 仍只接受 v3 lock path。无 v4 VAD artifact、无 ASR model load、四格均未运行、无 parser/publication；ignored sanitized invalid record SHA-256 `47299ae4a7d52b818dae72afa4e69f429e8ddd4e8a8a561ff5a96d0d894013c0`。V4 授权已消耗且不得原地修复或重试。
- Phase 3D v5 已完成仅限 task-local 的 lock-selection 修复并通过独立 Gate D5。新 lock 为 `research/gpu-short-parity-bisect-lock-v5.md`，SHA-256 `6a2ee4ee58cde783be473e907a4fe56aa72c7afcdba7b567644d2dfce74ac8fb`：VAD preflight 与 publisher 仅接受 canonical exact-v5 lock；v1-v4、unknown v6、cross-root same-name、malformed child path 和 wrong audio 均在 identity load 或 faster-whisper/ORT import 前拒绝。C++ runner/worker、两个 CT2 runtime、production fallback invariant、protocol 与默认路线保持 v4 identity 不变。两个 runtime smoke 与四个 v5 closed preflight 重现冻结 hash，全部明确 `vadModelLoaded=false` / `asrModelLoaded=false` / `modelLoaded=false`；独立 review 结论为 `clean-to-request-fresh-v5-acquisition`。
- 用户随后明确批准 exact v5 acquisition。模型前 re-attestation 通过，ignored JSON SHA-256 `d9414b5ce92f772b011c5509b40b513b07f897d360b4a82e4f60adaf81eb00bb`；fresh Silero V6 preflight 也通过并发布 SHA-256 `90e58fed46551047a55d89a537a8d91a692a844d73b6041c5d87ca4393b12002`，重现完整 `[0,385637)`、冻结 waveform 与 exact ORT loaded/not-loaded 闭集。首格 `python-runtime-python-mel` 成功构造 large-v3 CTranslate2 model 后、encode/generate 前以 `config_identity_mismatch` fail-closed：冻结 probe 输入为 `80×3000` Mel，而冻结 large-v3 model 实际暴露 `128` Mel bins（`preprocessor_config.json: feature_size=128`）。无 atomic row、parser 或 publication，其余三格未运行；ignored sanitized invalid record SHA-256 `3c8ebeddabe4b3e68b8c426a256293fd4cc0cc9d00d18d84e6cbaf1fff185d2c`，tracked report SHA-256 `725e6da1e3a0d7929d167d9455dcec3489847dadd9311217fed23b9897858676`。V5 authorization 已消耗，结论为 `invalid-evidence`，不得原地修复、重试或声称 feature/runtime/parser attribution。

## Authority And Dependencies

- 依赖归档 T06 `native-asr-ctranslate2-whisper` 的 production worker、诊断和 evidence seam；不得修改其 Candidate A/B artifacts。
- 依赖归档 T07 `native-asr-ctranslate2-cuda-development` 的 development GPU seam；该 seam 不是正式设备或 pack qualification。
- 字幕质量 authority 为 `.trellis/tasks/archive/2026-08/08-18-native-asr-python-legacy-baseline/research/python-legacy-baseline.json`。
- 当前 native disposition authority 为归档 `08-19-native-asr-legacy-quality-reevaluation`；large-v3 Candidate A 为 `stop-revise`。
- 遵循 `.trellis/spec/asr/quality-guidelines.md` 的完整矩阵、identity、reference 和绝对门禁合同。

## Requirements

1. 冻结新的 candidate/algorithm/config/binary/model identity，不复用 Candidate A/B 名义发布新结论。
2. 对归档重评中 large-v3 的每项 S/D/I/CER 回退给出可审计根因和对应修订，不得从 Python transcript 或 ASS reference 生成、补写或修复候选输出。
3. `large-v3` 与 `large-v2` 分别完成 short-v1、medium-v1、long-v2；单 case 质量失败不得截断剩余矩阵。
4. 每个 anchor 只与自己的同模型同 case Python row 比较，并在 `native-gpu-authoritative-v1` GPU identity 下独立通过 timeline、UTF-8、text conservation、protocol、accelerated RTF、wall、RSS、identity、path、取消/恢复、隐私和许可证门禁。
5. T07 GPU FP16 用于候选诊断和正式算法矩阵；T14/T15 仍须以最终 GPU pack identity 重做 pack/device attestation。CPU 不运行模型资格矩阵，匹配同模型、同算法的 CPU route 仅继承最终 GPU disposition，并明确记录 `inherited-from-gpu`。
6. 两个 anchor 未全部通过前，不解锁其他 ordinary Whisper 模型，也不改变 production/default Python legacy route。
7. 候选选择必须从历史 evidence 能支持的最小因果变更开始；每个候选在首次正式 model-backed acquisition 前冻结 identity，并按完整六行 anchor 矩阵收口，不能在看到单 case 结果后原地调参或拼接不同 identity。
8. 在冻结正式候选前，使用 T07 GPU FP16 做不具资格效力的最小诊断：short-v1 只比较 beam 1/5，因为单窗口没有 previous-text history 差异；medium-v1 比较 `beam 1/5 × history off/on` 四组合。其余算法、模型、音频、timestamp-driven seek、no-VAD、runner 和 runtime identity 保持一致。诊断只负责选择一个配置，不能与正式矩阵混用，也不能发布 qualified/accepted 结论。
9. 诊断选择规则必须在运行前冻结：候选组合先要求 large-v3 short-v1 与 medium-v1 的 CER、S/D/I、空文本、semantic gaps 和结构门禁全部不劣于匹配 Python row，并满足 GPU 绝对门禁；多个组合合格时选择 medium-v1 GPU inference RTF 最低者，再以 beam 较小、history off 作为确定性 tie-break。没有组合合格时发布 `no-candidate-selected` 并返回规划，不冻结正式候选，也不运行正式六行矩阵。
10. 诊断选中配置后，以新的 candidate lock 和重建 worker/runtime identity 重跑正式 short/medium；诊断 row 不得晋升、复制或作为正式矩阵 row。
11. 首轮 Gate A 的 `no-candidate-selected` 已关闭 beam/history-only scope。用户批准的第二轮 exact-VAD 两行诊断也已发布 `no-candidate-selected`：VAD 不改变 short insertion blocker，并以 medium 文本全面回退换取 gap 清零。该 scope 至此关闭；不得冻结正式候选或运行正式六行矩阵。其余 beam、prompt seed、word timestamps、seek overlap、VAD 参数、post-processing 和 reference-derived repair 仍未授权。
12. 已批准并完成不具资格效力的 upstream parity 研究。研究只允许提出 `upstream-generation-fallback-parity-v1`：在第二轮 beam5/history-on/exact V6 VAD identity 上增加 faster-whisper 1.2.1 的固定 generation fallback ladder，并让实际选中 temperature 控制既有 `>0.5` prompt reset。它不是 temperature 搜索，不得同时修正 history eligibility 或其他变量。
13. 用户已批准 Phase 3C pre-model implementation：实现 exact upstream fallback helper、fake-generation Python-oracle parity tests、闭集 identity/trace schema，并准备两行新 lock；批准不包含模型加载、第三轮 GPU acquisition、production default 修改或 history-eligibility 修订。Phase 3C 必须经独立 pre-model review 后再次停止并请求 acquisition 决策。
14. 已批准的两行 Phase 3C acquisition 因冻结 lock 缺少 publisher 强制要求的直接 loaded-module hash inventory，在模型加载前判为 invalid evidence。不得在 lock `5fcfe845...` 下继续 acquisition 或伪造 publication。v2 lock `823630f0...` 虽列出完整 inventory，但 Gate C3 review 证明 publisher 尚未强制 exact set，因此同样不得 acquisition。最终 reviewed identity 为 `gpu-fallback-diagnostic-lock-v3.md` / `b95bdcc1...`；它通过 fallback-only exact module-set mutation tests，且已在 fresh explicit approval 后完成两行 acquisition。
15. v3 fallback diagnostic 已发布 `no-candidate-selected`：short 未触发 fallback 且保留 insertion blocker，medium 实际触发 fallback 但 CER/deletions/insertions/semantic gaps 回退。该 single-causal candidate scope 至此关闭；不得冻结正式 candidate、运行 Phase 4/六行正式矩阵或把诊断 row 晋升。下一步必须返回规划，不能原地修改 v3 identity 或混入 history eligibility/其他变量。
16. 冻结任何下一 quality candidate 前，必须先定位 mandatory short 的首个 parity divergence stage。诊断须把 waveform/VAD、Mel、prompt/suppression/options、CT2 binary/loaded modules、first-attempt/fallback token aggregates 与 Python/native timestamp parser 分开绑定；fresh Python 输出只验证既有 baseline 可复现性和 stage parity，不得成为新 reference 或质量期望。
17. 已批准的 Phase 3D pre-model scope 固定为 `short-input-runtime-parity-bisect-v1`：四格仅改变 Mel producer 与 CT2 runtime 两个正交因素，使用同一 test runner/harness、short waveform、large-v3 snapshot、prompt/options/fallback/parser contracts。实现必须默认关闭、task-local、promotion-ineligible；若 Python-wheel CT2 不能通过同一 runner 的 no-model ABI/runtime smoke，须停止并返回设计，不得静默改成不同 Python harness。Phase 3D 经独立 review 后必须请求单独 acquisition 授权。
18. Phase 3D Gate D 已在 lock `470f9e0f...` 下 clean。所有模型前身份、四格映射、runtime 闭集、Mel、parser、fallback fingerprint、privacy 与 promotion rejection 已通过独立复核；下一步只可请求并等待四格 acquisition 明确授权。既有任何授权不得继承，clean review 本身不授权模型加载。
19. 用户对 `470f9e0f...` 的 acquisition 授权因 VAD identity/preflight artifact 未被该 lock/publisher 直接绑定而在模型加载前失效。不得在旧 lock 下运行 VAD 或四格 acquisition；须保持旧 lock byte-identical，新建冻结 Silero V6/source/ONNX Runtime 与实际 sanitized preflight artifact 的 v2 identity，经 no-model tests 和独立 review 后重新取得明确授权。
20. Corrected v2 lock `481c17cc...` 已通过独立 Gate D2；review 只允许请求 renewed acquisition。不得继承旧授权，也不得在用户对“exact v2 VAD preflight + 四格”明确批准前加载 VAD/ASR 模型。
21. 用户批准的 exact v2 acquisition 已在 VAD 实际执行后的 module attestation 处判为 invalid evidence：Python ORT 未加载 lock 要求的 sibling `onnxruntime.dll`。不得在 `481c17cc...` 下继续、放宽校验或运行四格；任何修复必须创建新的 reviewed lock，绑定实际 Python ORT loaded-module contract，并重新取得明确授权。
22. V3 lock `62ef486a...` 已通过独立 Gate D3；review 只允许请求 fresh acquisition。实际合同冻结 `.pyd + providers_shared.dll` 为 exact loaded set、`onnxruntime.dll` 为 exact not-loaded disk identity；missing/additional/unresolved-path/root/hash/status drift 均拒绝。不得继承旧授权，仍须 fresh explicit v3 authorization 才可运行 VAD 或四格。
23. Fresh v3 authorization 已在通过 VAD preflight 后由首格 pre-model config invariant 消耗：冻结 runner 的 parity path 未向 fallback-enabled backend 提供 VAD model path，故在 CT2 Whisper model construction 前 fail-closed。不得在 `62ef486a...` 下修改 source、放宽 backend invariant、补传 path 或继续其余三格。任何修复必须重建 runner、冻结新 lock、完成独立 no-model review 并再次获得明确授权。
24. V4 lock `e38e23f5...` 已通过独立 Gate D4；review 只允许请求 fresh v4 acquisition。未来命令必须为 preflight 和每个 parity cell 提供对应 isolated runtime bin 内的 exact `--vad-model`，missing/cross-root/name/size/hash/old-lock drift 均须在 backend construction 前拒绝。不得复用 v3 VAD artifact 或继承任何旧授权，仍须 fresh explicit v4 authorization 才可运行 VAD 或四格。
25. Fresh v4 authorization 已由 exact VAD preflight 的冻结 lock-path 校验消耗：v4 identity 绑定的 `whisper_short_vad_preflight.py` 仍只接受 v3 lock path，因此在任何 VAD/ASR model load 前 fail-closed。不得在 `e38e23f5...` 下修改 tool、放宽 path 校验、改用 v3 artifact 或继续四格；任何修复必须创建新的 reviewed lock/tool identity 并重新取得明确授权。
26. V5 lock `6a2ee4ee...` 已通过独立 Gate D5；review 只允许请求 fresh v5 acquisition。不得继承 v4 或更早授权。只有用户对 exact v5 VAD preflight 与四个 closed model-backed cells 再次明确批准后，才可执行 Silero V6、CTranslate2 model construction、encode/generate、parser replay 或 attribution publication。
27. Fresh v5 authorization 已在首格 model-backed entry 的 Mel/model shape contract 处消耗：VAD 与 large-v3 model construction 均成功，但冻结 `80×3000` probe 无法输入实际 `128`-mel large-v3 model，故 encode/generate 前以 invalid evidence 停止。不得在 `6a2ee4ee...` 下修改 shape、替换 Mel、放宽 model contract、继续其余三格、运行 parser/publisher 或创建 v6；任何后续工作必须先返回规划，纠正 feature producer 与 model `n_mels` 的 authority，再形成新的 reviewed scope 和 fresh authorization。

## Acceptance Criteria

- [ ] 首轮诊断 lock、六个预定组合、选择规则和 `no-candidate-selected` 路径通过 mutation/determinism tests；第二轮 VAD 两行使用独立 lock/schema/hash 并通过同等 identity、privacy、promotion-rejection 和 byte-determinism checks；正式 publisher 拒绝任一诊断 row promotion。
- [ ] 选中的新 candidate identity 与全部输入、GPU runtime、binary、两个 model hashes 已冻结并通过 mutation/identity validation。
- [ ] `large-v3` 和 `large-v2` 在同一冻结 algorithm/config/runtime candidate 下各有完整的 short-v1/medium-v1/long-v2 GPU rows，每个 anchor 绑定自己的精确 model identity，共六个 case disposition；无外部终止或损坏 row 被当作完成证据。
- [ ] 每个 GPU case 的 CER、S/D/I、空文本和 semantic confirmed-speech gaps 均不劣于匹配的 `python-legacy-cuda-v1` row。
- [ ] 两个 anchor 的 GPU rows 均通过所有独立绝对硬门禁；任一失败时发布完整 `stop-revise`，不声称 ordinary Faster-Whisper qualified。
- [ ] CPU route 不包含伪造或复制的测量值；只有 GPU 六行矩阵通过后才记录 `inherited-from-gpu`，并绑定完全相同的 model/algorithm identity。
- [ ] 历史 T06/T07 artifacts 和结论无修改，T07 evidence 明确保持 development-only。
- [x] 已生成 `research/whisper-quality-non-qualified-handoff.md`，供父任务/T14/T15 识别为无 accepted ordinary Faster-Whisper input；未把任何 diagnostic、invalid parity row 或中间状态冒充 release input。

## Forbidden Premature Claims

- 不得在 GPU 双 anchor 全部通过前声称 ordinary Faster-Whisper、其他 Whisper 模型或 native release qualified。
- CPU 的 `inherited-from-gpu` 是用户批准的资格继承政策，不是 CPU 实测；不得发布 CPU CER/S/D/I/RTF/RSS 数值或声称存在 CPU model-backed evidence。
- 不得把 development GPU speed evidence、旧 absolute gate、Candidate A retained output 或 Candidate B diagnostics 当作当前 release qualification。

## Out Of Scope

- T12 模型下载/manifest、T13 runtime packaging、T14/T15 pack/device qualification。
- Kotoba K3、Qwen、Parakeet、ReazonSpeech 质量工作。
- 设置、前端、安装器、production route 切换或 Python legacy 移除。
